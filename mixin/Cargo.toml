[package]
name = "retros-mixin-api"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Stable C-ABI contract between the RetrOS core and `kind = \"mixin\"` packages."

[lib]
path = "src/lib.rs"
